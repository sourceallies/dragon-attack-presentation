type Query {
    gameCharacter(id: ID!): GameCharacter!
}

type Mutation {
    attack(targetId: ID!): Int!
}

type GameCharacter {
    currentHealth: Int!
}

type HealthChangeEvent {
    newHealth: Int!
}

type Subscription {
    watchCharacter(targetId: ID!): HealthChangeEvent!
}